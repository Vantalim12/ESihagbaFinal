# Use mainnet backend when running frontend locally (npm run dev).
# Set to "true" or "1" to connect to https://ic0.app instead of local replica.
# VITE_USE_MAINNET=true

# Staging: use local backend canister when testing against local replica.
# Get ID with: dfx canister id backend
# VITE_BACKEND_CANISTER_ID=rrkah-fqaaa-aaaaa-aaaaq-cai
